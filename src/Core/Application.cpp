//#include <print>

/*static int s_AllocationCount = 0;

void* operator new(size_t size)
{
    s_AllocationCount++;
    //printf("Allocated size of %zu\n", size);
    return malloc(size);
}*/

//std::println("Number of allocations: {}", s_AllocationCount);